{
  "name": "i18n-sharpen-docs",
  "version": "1.0.0",
  "description": "Documentation website for i18n-sharpen",
  "type": "module",
  "scripts": {
    "dev": "vitepress dev",
    "build": "vitepress build",
    "preview": "vitepress preview",
    "deploy": "pnpm run build && npx wrangler pages deploy .vitepress/dist --project-name i18n-sharpen-docs"
  },
  "dependencies": {
    "vitepress": "^1.2.3",
    "vue": "^3.4.27"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}
